create table Movie (
                       id int8 not null,
                       name varchar(255),
                       primary key (id)
);
create sequence hibernate_sequence start 1 increment 1;
